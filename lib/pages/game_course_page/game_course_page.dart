import 'package:flutter/material.dart';

class GameCoursePage extends StatelessWidget {
  const GameCoursePage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('GameCoursePage'),
      ),
    );
  }
}
